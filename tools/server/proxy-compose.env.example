# Copy this file to tools/server/.env and then edit the values.

# Optional host IP to publish on. Leave at 127.0.0.1 for local-only access.
TS_IP=127.0.0.1

# Optional Hugging Face token for backend -hf downloads.
HF_TOKEN=

# Persistent Hugging Face cache on the host.
HF_CACHE_DIR=/home/your-user/.cache/huggingface

# Public API key exposed by the proxy.
PUBLIC_API_KEY=replace-with-a-long-random-public-key

# Backend model + runtime tuning (safe defaults for 8 GB VRAM).
MODEL_REF=bartowski/Mistral-Nemo-Instruct-2407-GGUF:Q2_K
MODEL_ALIAS=mistral-nemo-32k
N_CTX=32768
N_PARALLEL=1
N_BATCH=1024
N_UBATCH=256
N_GPU_LAYERS=all
FIT_MODE=on
FIT_TARGET_MIB=256
KV_CACHE_K_TYPE=pq3_5
KV_CACHE_V_TYPE=q8_0
